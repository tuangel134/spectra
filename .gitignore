node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Local Spectra runtime state (personal — never publish)
.spectra/sessions/
.spectra/specs/
.spectra/headroom/
.spectra/evals/
.spectra/autorun/
# Local working config (ship spectra.example.jsonc instead)
/spectra.jsonc

# Native desktop (Rust) build artifacts
desktop-native/target/

# VS Code extension build output / packages
vscode-extension/out/
*.vsix

# Spectra Core durable local state
.spectra/state/

# Local transactional upgrade backups
.spectra-upgrade-backup/
